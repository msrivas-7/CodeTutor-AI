# Production-side module — main.py imports from here and tests it.
#
# Define two functions:
#
#   add(a, b) -> int
#   is_even(n) -> bool
#
# These are deliberately trivial so the lesson is about *writing tests*,
# not figuring out the implementation.

def add(a, b):
    # TODO: return a + b
    pass


def is_even(n):
    # TODO: return n % 2 == 0
    pass
