inputs = ["42", "hello", "7", "", "-3"]

parsed = []
skipped = []

# TODO: loop over `inputs`. Try int(s) — append the result to `parsed` on
# success, and append the original string `s` to `skipped` on ValueError.

print(f"parsed: {parsed}")
print(f"skipped: {skipped}")
