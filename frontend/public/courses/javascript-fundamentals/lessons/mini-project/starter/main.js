// Mini project: habit tracker.
// Fill in each function, then wire them together in main().

function addHabit(habits, name) {
  // TODO: return habits with { name, done: false } appended
}

function markDone(habits, name) {
  // TODO: return habits with the matching habit's done flipped to true;
  //       if no habit matches, return the list unchanged.
}

function summary(habits) {
  // TODO: return a string like "1 of 3 habits done"
}

function main() {
  // TODO: build a list with three habits ("read", "walk", "meditate"),
  //       mark "walk" done, print each as "name: done" or "name: todo",
  //       then print the summary string.
}

main();
