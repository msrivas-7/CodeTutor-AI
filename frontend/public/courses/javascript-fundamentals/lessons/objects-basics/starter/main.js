// Write a function `formatPerson(person)` that takes an object with
// `name` and `age` properties and returns a string like
// "Ada is 36 years old."

function formatPerson(person) {
  // TODO: return the formatted string
}
