// Write a function named greet(name) that RETURNS "Hello, <name>!".
// The checker will call greet(...) for you — don't console.log the result.

function greet(name) {
  // TODO: return the greeting
}
